namespace Functions_Logic.Utilities
{
    public enum Accumulation
    {
        NotSpecified = 0,
        Cumulative = 1,
        Annualised = 2,
       
    }

    public enum Rebalancing
    {
        NotSpecified = 0,
        NoRebalancing = 1,
        Rebalance = 2
    }
}
